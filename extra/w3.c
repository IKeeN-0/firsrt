#include <stdio.h>
int main(){
    int arr[10];
    for (int i = 0; i < 10; i++)
    {
        scanf("%d",&arr[i]);
    }
    int mode = 0;
    for (int i = 0; i < 10; i++)
    {
        for (int j = 0; j < 10; j++)
        {
            if (arr[i] = arr[j])
            {
                
            }
            
        }
        
    }
    printf("Mode = %d",mode);
}